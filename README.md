# aleotoolz.xyz
Aleo scripts

```
apt install pssh # required for restart_miner.sh, confirmed_blocks.sh

pip install -r requirements.txt

echo 'ssh-alias-01;ipaddr-01;aleo01...' >> hosts.txt
echo 'ssh-alias-02;ipaddr-02;aleo02...' >> hosts.txt

python stats.py
```
